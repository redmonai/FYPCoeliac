<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ailbh.fypcoeliac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DietInfoScreen" />
        <activity android:name=".cd_info_screen" />
        <activity android:name=".LabellingInfoScreen" />
        <activity android:name=".OatsInfoScreen" />
        <activity android:name=".DisclaimerScreen" />
        <activity android:name=".CDInfoScreen" />
        <activity
            android:name=".main_navdrawer"
            android:label="@string/app_title"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".search_results"></activity>
    </application>

</manifest>